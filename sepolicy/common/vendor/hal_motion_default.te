hal_attribute(motion)

type hal_motion_default, domain;
type hal_motion_default_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(hal_motion_default)

hal_server_domain(hal_motion_default, hal_motion)
