# ==========================================
# hal_vibrator_default.te
# ==========================================
# Vibrator control is typically mapped through the LED subsystem in the kernel
allow hal_vibrator_default sysfs:file { open read write };
allow hal_vibrator_default sysfs_leds:dir search;
