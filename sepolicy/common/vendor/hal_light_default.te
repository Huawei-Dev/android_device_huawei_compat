# ==========================================
# hal_light_default.te - Notification LEDs
# ==========================================
# Allow the light HAL to write to sysfs nodes to toggle LEDs and brightness
allow hal_light_default sysfs:file { getattr open read write };
