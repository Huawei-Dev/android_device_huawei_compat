# ==========================================
# hal_bsoh_default.te - Vendor Helper Service
# ==========================================

# Define the binary and the domain
type hal_bsoh_default, domain;
type hal_bsoh_default_exec, exec_type, vendor_file_type, file_type;

# Standard transition from init to bsoh domain
init_daemon_domain(hal_bsoh_default)
